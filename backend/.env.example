DATABASE_URL=sqlite:///./quiz_face.db
CORS_ORIGINS=http://localhost:3000
UPLOADS_DIR=../uploads

# face_recognition settings
FACE_TOLERANCE=0.5          # lower = stricter match (0.4–0.6 is typical)
FACE_DETECTION_MODEL=hog    # "hog" for CPU, "cnn" for GPU
